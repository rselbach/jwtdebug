version: "2"

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - ineffassign
    - unused
  settings:
    errcheck:
      exclude-functions:
        - fmt.Fprintf
        - fmt.Fprintln
        - fmt.Fprint
        - (*os.File).Close
        - io.Copy
        - os.RemoveAll
        - os.Remove

formatters:
  enable:
    - gofmt
  settings:
    gofmt:
      simplify: true
